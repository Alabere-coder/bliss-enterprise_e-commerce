import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "m.media-amazon.com", "plus.unsplash.com"],
  },
};

export default nextConfig;
